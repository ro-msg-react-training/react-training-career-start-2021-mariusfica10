const initState = {
    posts: [
        {id: '1', title: 'dasd'},
        {id: '2', title: 'daaddsd'},
        {id: '3', title: 'dadaddsd'}
    ]
}

const rootReducer = (state = initState, action) =>
{
    return state;
}

export default rootReducer